#!/bin/bash
set -e

docker-compose -f docker-compose-test.yml up --remove-orphans -d db-test
docker-compose -f docker-compose-test.yml up --remove-orphans --abort-on-container-exit api-test
docker-compose -f docker-compose-test.yml up --remove-orphans --abort-on-container-exit web-test
