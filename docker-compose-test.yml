version: '3'
services:
  api-test:
    build: api
    command: go test ./...
    depends_on:
      - db
      - rails-db
    environment:
      - DATABASE_URL=postgres://dawnpatrol@db/dawnpatrol_test?sslmode=disable
  db:
    build: db
  rails-db:
    build: rails-db
  web-test:
    build:
      context: web
      dockerfile: Dockerfile-test
